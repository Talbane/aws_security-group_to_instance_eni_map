# aws_security-group_to_instance_eni_map.py

This will take the security group ID as the input, take that in turn return that details of which Instance and ENI that the Security Group is associated with along with giving you the Name of the Security Group 
